@model IEnumerable<gestionPharmacieApp.Models.Facture>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdVenteNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdVenteNavigation.IdVente)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFacture">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFacture">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFacture">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
